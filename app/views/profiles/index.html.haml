- content_for :title do
  = "People" 

%h1 People

.listings
  - @profiles.each do |profile|
    .individual-listing
      .row-fluid
        .span12
          %h3= link_to "#{profile.first_name} #{profile.last_name}", profile_path(profile)  
          %h4  #{profile.hometown}, #{profile.nationality}

          .row-fluid
            .span5
              = link_to profile_path(profile[:id]) do
                - if profile.assets.empty?
                  = image_tag("default_listing_image.gif") 
                - else
                  = image_tag profile.find_active.file_url
            .span7   
              %p Skills: #{profile.skills} 
              %p About: #{profile.about}
  .pagination.pagination-large.pagination-centered
    %ul
      %li
        %a{:href=>""} Prev
      %li
        %a{:href=>"#"} 1
      %li
        %a{:href=>"#"} 2
      %li
        %a{:href=>"#"} Next


