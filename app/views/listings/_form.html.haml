= render 'shared/errors', f: @listing
= form_for @listing do |f|
  %br
  .span.offset2
    .field
      = f.label :title
      = f.text_field :title, :class => "wider-input"
    %br
    .field
      = f.label :city
      = f.text_field :city
    %br
    .field
      = f.label :description
      = f.text_area :description, :class => "wider-input taller-input"
    %br
    .field
      = f.label :number_of_guests
      = f.number_field :guests, :class => "number-input"
    %br
    .field
      = f.label :stay_length
      = f.text_field :stay_length, :class => "wider-input"
    %br
    .field
      = f.label :country
      = f.country_select :country
    %br
    .actions
      = f.submit "List Hosting", :class => "btn btn-info"
